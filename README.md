```bash
    uv add google-adk
    uv add litellm #  open-source proxy server for cally many different LLM APIs
    
    adk create weather_agent
    adk run weather_agent # command line
    adk web --port 8080 # web interface 
    # standalone main
    python main.py
    
```